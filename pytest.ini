[pytest]
minversion = 7.0
addopts = -q
testpaths =
    tests/unit
    tests/integration
python_files = test_*.py

norecursedirs =
    .git
    .venv
    tests/integration/legacy

markers =
    integration: mark tests as integration tests.
