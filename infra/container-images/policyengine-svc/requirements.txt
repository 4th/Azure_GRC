 # --- Core runtime (PolicyEngine service) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
PyYAML>=6.0.0
requests>=2.31.0

# --- Scorecard / UI ---
streamlit>=1.37.0

# --- Azure integrations (Blob, Cosmos, Key Vault, Identity) ---
azure-storage-blob>=12.20.0
azure-cosmos>=4.7.0
azure-identity>=1.17.0
azure-keyvault-secrets>=4.8.0

# --- Semantic Kernel / Agentic integration ---
semantic-kernel>=1.0.0

# --- Optional config helpers ---
python-dotenv>=1.0.1

# --- Linting, formatting, type checking ---
black>=24.3.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.10.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0

# --- Security / YAML / secrets scanning ---
bandit>=1.7.9
yamllint>=1.35.0
detect-secrets>=1.5.0
